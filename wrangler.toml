name = "telegram_private_chatbot"
main = "worker.js"
compatibility_date = "2025-06-01"

# 注意：KV 命名空间 ID 每个用户都不一样，
# 所以不能写死在这里。用户需要在 Cloudflare 后台手动绑定。

# --- 监控与可观测性 ---
[observability]
enabled = true

[observability.logs]
enabled = true
head_sampling_rate = 0.1  # 10% 采样率（避免高频日志产生过多费用）
